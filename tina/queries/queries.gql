query pageQuery {
  postConnection {
    edges {
      node {
        id
        date
        title
        excerpt
        author
        _sys {
          filename
        }
      }
    }
  }
}

query blogPostQuery($relativePath: String!) {
  post(relativePath: $relativePath) {
    ...PostParts
  }
}
